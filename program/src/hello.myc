fn_main {
  fn_putchar(65)
  fn_putchar(66)
  fn_putchar(67)
  fn_putchar(68)

  for (;zero==zero;) {
    a = 1
  }
}

fn_putchar {
  push(a,b,i)
  i = sp + 4
  a = mem[i]

  b = 1
  for (;b == 1;) {
    b = monitor_busy()
  }

  monitor(a)
  pop(i,b,a)
}
