def main() {
  a = 72
  call(label_putchar)

  a = 69
  call(label_putchar)
  
  halt()
}

def label_putchar() {
  push(a,b)
  b = 1
  for (;b == 1;) {
    b = monitor_busy()
  }
  monitor(a)
  pop(b,a)
  return
}
